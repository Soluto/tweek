machine:
  environment:
    NODE_ENV: development
  node: 
    version: 6.9.1
dependencies:
  pre:
    - npm install yarn -g
  override: 
    - yarn
  cache_directories:
    - ~/.yarn
    - ~/.yarn-cache   
general:
  build_dir: BackOffice
