machine:
  services:
    - docker
  environment:
    NODE_ENV: development
    YARN_VERSION: 0.17.8
    PATH: "${PATH}:${HOME}/.yarn/bin"
  node: 
    version: 6.9.1
dependencies:
  cache_directories:
    - ~/.yarn
    - ~/.yarn-cache
  pre:
    - ls -a
    - ls ~/ -a
    - yarn cache dir
    - mkdir -p ~/.yarn && cd ~/.yarn && wget https://yarnpkg.com/latest.tar.gz && tar -xf latest.tar.gz dist --strip-components=1
    - cat CircleDockerfile > Dockerfile
    - docker build --rm=false -t tweek .
  override:
    - yarn install
test:
  override:
    # - yarn test:unit
    #- wget https://selenium-release.storage.googleapis.com/3.0/selenium-server-standalone-3.0.1.jar
    #- java -jar selenium-server-standalone-3.0.1.jar:
    #      background: true
    #- wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
    #- sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
    #- sudo apt-get update
    #- sudo apt-get --only-upgrade install google-chrome-stable
    #- npm install selenium-standalone -g
    - docker images
    - docker run -d -p 8080:3000 tweek
    #- npm start
    - curl --retry-delay 5 --retry 10 -v http://localhost:8080/keys
    - npm run test:e2e
general:
  build_dir: BackOffice
deployment:
  production:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push tweek
