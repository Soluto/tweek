machine:
  environment:
    NODE_ENV: development
  node: 
    version: 6.9.1
dependencies:
  pre:
    - npm install yarn -g
    - echo @@@@@@@@@@@@@pre
    - ls -a
  override: 
    - cd BackOffice && yarn
  post:
    - echo @@@@@@@@@@@@@post
general:
  build_dir: BackOffice
